gschem schematic/symbol -> scheme script

Usage: gaf shell sch2scheme.scm schematic-name.sch scheme-name.scm [scale-factor]

scheme-name.scm then contains script which can reproduce the
initial schematic. Running it using 'gaf shell scheme-name.scm'
outputs the file 'tmp.sym' identical with initial.schematic.
